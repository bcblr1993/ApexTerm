# ApexTerm 全量版本发布测试规范与验收场景矩阵 (TEST_SCENARIOS)

> **发布准则**：本规范参考 GitHub 知名开源终端 **electerm**、**FinalShell**、**Termius**、**Tabby** 等主流 SSH 终端软件的核心功能架构与生产事故复盘整理。
> **每次正式版本发布前，必须完整通过本测试矩阵中的所有 P0（阻塞级）与 P1（严重级）场景。若有任意一项 P0 失败，严禁打包发布！**

---

## 一、 测试分级与发布门禁机制 (Release Gates)

| 等级 | 标识 | 定义 | 发布门禁要求 |
| :--- | :--- | :--- | :--- |
| **P0** | 🔴 阻塞 (Blocker) | 导致软件崩溃、数据丢失、SSH 连不上、密码泄露、UI 卡死、无法退出的致命问题 | **100% 通过**，0 容忍 |
| **P1** | 🟠 严重 (Critical) | 核心功能受损、终端花屏、乱码、SFTP 传输失败、快捷键失效、内存泄漏 | **100% 通过** |
| **P2** | 🟡 一般 (Major) | 次要功能异常、边缘格式解析偏差、文案缺失、样式轻微错位 | 允许有已知排期，通过率 ≥ 95% |
| **P3** | 🟢 轻微 (Minor) | 提示细节、操作手感优化、极端低频场景建议 | 不阻塞发布，纳入后续迭代 |

---

## 二、 全量核心测试场景矩阵 (8 大维度 86 个测试用例)

### 模块 1：SSH 连接与多模态身份认证 (SSH Connection & Authentication)

| 用例 ID | 场景描述 | 优先级 | 测试步骤 / 验证方法 | 预期结果 |
| :--- | :--- | :---: | :--- | :--- |
| **TC-SSH-01** | 标准密码认证 | P0 | 输入常规主机 IP、端口 22、用户名及字母数字密码连接 | 握手顺畅，1 秒内成功获取远端 Shell 提示符 |
| **TC-SSH-02** | 复杂特殊字符密码登录 | P0 | 密码包含 `!@#$%^&*()_+~|}{[]:;?><,./'"`、空格、中文或超长 64 位 | 密码传参无转义截断，安全入库钥匙串并成功登录 |
| **TC-SSH-03** | RSA 私钥认证 | P0 | 配置 OpenSSH 格式或 PEM 格式 RSA 私钥 (2048/4096 位) | 免密直连成功，无需输入密码 |
| **TC-SSH-04** | Ed25519 现代私钥认证 | P0 | 配置 OpenSSH Ed25519 格式私钥连接现代 Linux (Ubuntu 22.04+) | 正常完成椭圆曲线非对称密钥协商并登入 |
| **TC-SSH-05** | 口令保护私钥 (Passphrase) | P1 | 加载带 Passphrase 密码保护的私钥文件连接 | 弹出输入 Passphrase 弹窗，输入正确登入；输入错误提示重试；取消中止连接 |
| **TC-SSH-06** | macOS Keychain 凭据隔离 | P0 | 添加会话保存密码，重启 App 后重新连接 | 密码从系统钥匙串安全解出，不写明文 sessions.json，无二次输入密码弹窗 |
| **TC-SSH-07** | 非标准 SSH 端口 | P0 | 连接配置在 `2222`、`52022` 等高位自定义端口的服务器 | 正确建立 TCP 连接与 SSH 隧道，无端口阻断 |
| **TC-SSH-08** | IPv6 地址直连 | P1 | 输入标准 IPv6 格式地址（如 `[2001:db8::1]` 或纯文本） | 正确解析 IPv6 路由并发起 SSH 握手 |
| **TC-SSH-09** | 域名与 `/etc/hosts` 解析 | P1 | 使用 `example.com` 或本地 `/etc/hosts` 自定义别名主机名连接 | 本地 DNS/hosts 解析正常并连接成功 |
| **TC-SSH-10** | 跳板机 / 堡垒机连接 (JumpHost) | P1 | 配置 `ProxyJump` 跳板机（A 跳 B）后连接内网目标服务器 | 成功穿透跳板机，终端呈现目标内网机器提示符 |
| **TC-SSH-11** | SOCKS5 / HTTP 局部代理透传 | P1 | 配置应用代理服务器连接受限内网节点 | 流量经代理协议路由转发并成功登录 |
| **TC-SSH-12** | 首次连接 Host Key 指纹确认 | P0 | 首次连接未知主机，触发 `known_hosts` 指纹验证 | 弹出指纹比对提示（SHA256:xxx），确认后记录，不再重复提示 |
| **TC-SSH-13** | 中间人攻击防范 (Host Key 变更) | P0 | 目标机重装导致 Host Key 变化时连接 | 立即弹出醒目红色安全拦截警报，阻止连接，防止 MITM 篡改 |
| **TC-SSH-14** | 异常连接超时防护 | P0 | 输入一个不可达的保留 IP (如 `192.0.2.1`) 或已被防火墙静默丢弃的 IP | 5 秒后抛出明确超时提示，UI 线程绝对不可转圈卡死 |
| **TC-SSH-15** | 心跳保活与睡眠唤醒重连 | P0 | 持续挂机 30 分钟 / MacBook 盖盖休眠 10 分钟后开盖 | 自动发送 KeepAlive 保活不掉线；若已断网，UI 显式标明断开并提供一键重连 |

---

### 模块 2：终端模拟引擎与渲染 (Terminal Emulation & Rendering)

| 用例 ID | 场景描述 | 优先级 | 测试步骤 / 验证方法 | 预期结果 |
| :--- | :--- | :---: | :--- | :--- |
| **TC-TERM-01** | ANSI 16 色与粗体高亮 | P0 | 执行 `msgcat --color=test` 或基础 ANSI 测试脚本 | 16 种色彩渲染无色偏，粗体亮色对比分明 |
| **TC-TERM-02** | ANSI 256 色色板渲染 | P0 | 运行 `curl -sL https://raw.githubusercontent.com/chriskempson/base16-shell/master/profile_helper.sh` | 256 色阶平滑过渡，无黑块或颜色错位 |
| **TC-TERM-03** | 24-bit TrueColor (全彩) | P1 | 运行 `printf "\x1b[38;2;255;100;0mTRUECOLOR\x1b[0m\n"` 及渐变脚本 | 真实 1670 万色平滑渐变输出，无色阶断层 |
| **TC-TERM-04** | 文本样式修饰 | P1 | 输出下划线、斜体、删除线、反白 (Reverse) 字符 | AppKit/SwiftUI 文本容器精准渲染修饰样式 |
| **TC-TERM-05** | 备用屏幕缓冲区 (Alternate Screen) | P0 | 打开 `vim /etc/hosts`、`htop`、`tmux`、`less`，随后按 `:q` 退出 | 退出后瞬间还原进入前的终端历史，无残影或多余空行 |
| **TC-TERM-06** | 窗口动态缩放 (SIGWINCH 信号) | P0 | 左右拖拽调整窗口大小、分屏或全屏切换 | 远端 PTY 即时接收新行列数值，全屏程序重排自适应，无错位换行 |
| **TC-TERM-07** | CJK (中日韩) 双宽字符与标点 | P0 | 输出大量中文、日语假名、全角标点，输入光标移动穿透 | 中文字符占 2 个半角宽度，光标严格对齐，不发生字符撕裂 |
| **TC-TERM-08** | Emoji 与 Nerd Font / Powerline 图标 | P1 | 输出 `🚀⚡💻` 及 Oh-My-Zsh git 分支箭头、开发图标 | 图标居中显示、字距正常，无重叠或显示为问号方块 |
| **TC-TERM-09** | 中文输入法 (IME) 上屏与候选 | P0 | 使用系统原生拼音输入法输入长中文句子 | 候选词框定位准确，输入过程中无幽灵字符，回车/空格正常上屏 |
| **TC-TERM-10** | 鼠标选择与系统剪贴板互通 | P0 | 鼠标拖拽选择文本、双击选单词、三击选行，按 Cmd+C 与 Cmd+V | 复制内容准确无乱码进入系统剪贴板，粘贴无截断 |
| **TC-TERM-11** | 括号粘贴保护 (Bracketed Paste) | P0 | 复制一段包含多行 `rm -rf` 或换行的脚本直接粘贴到终端 | 识别括号粘贴转义，不直接自动逐行触发回车执行，防误操作 |
| **TC-TERM-12** | 鼠标事件跟踪 (SGR Mouse Mode) | P1 | 在 `htop` 中鼠标点击列头排序，在 `mc` 中点击菜单 | 远程控制台准确捕获鼠标点击坐标与滚动动作 |
| **TC-TERM-13** | 环形滚动历史 (RingBuffer) 淘汰 | P0 | 设置最大滚动历史 5000 行，持续输出 100,000 行文本 | 旧日志平滑丢弃，总行数稳定在上限，内存稳定不爆炸 |
| **TC-TERM-14** | 清屏指令响应 | P0 | 执行 `clear`、`printf '\033c'` 以及按快捷键 Cmd+K | 终端视图与滚动条立即清空，光标归位至左上角 |
| **TC-TERM-15** | 终端光标多形态切换 | P2 | 切换光标为方块 (Block)、竖线 (Beam)、下划线 (Underline) | 光标样式随设置实时生效，闪烁动画平滑无掉帧 |

---

### 模块 3：SFTP 文件管理与双向数据同步 (SFTP & File Transfer)

| 用例 ID | 场景描述 | 优先级 | 测试步骤 / 验证方法 | 预期结果 |
| :--- | :--- | :---: | :--- | :--- |
| **TC-SFTP-01** | 根目录与多层级深层浏览 | P0 | 导航至 `/`、`/var/log`、深层嵌套 10 层目录并返回 | 文件树加载完整，面包屑导航定位准确 |
| **TC-SFTP-02** | 软链接 (Symlink) 识别 | P1 | 浏览包含软链接的目录（如 `/bin` -> `usr/bin`） | 标识软链接图标，双击进入时能正确定位目标文件或目录 |
| **TC-SFTP-03** | 隐藏点文件 (.dotfiles) 显示 | P1 | 浏览用户主目录 `~`，查看 `.bashrc`、`.ssh` 等隐藏项 | 正确列出隐藏文件，支持按文件名过滤筛选 |
| **TC-SFTP-04** | 文件与目录权限查看/修改 | P1 | 右键查看文件属性，修改权限为 755 或 644 | 远端成功执行 chmod，文件列表属性即时刷新更新 |
| **TC-SFTP-05** | 基础文件系统操作 | P0 | 新建文件、新建文件夹、重命名、单文件删除、批量删除 | 操作完成无报错，文件列表即时同步刷新，删除有确认保护 |
| **TC-SFTP-06** | Finder 本地拖拽上传 | P0 | 从 macOS 桌面拖拽多个文件/大文件夹到 SFTP 视图 | 自动建立上传传输任务，目标路径完整，文件结构保持一致 |
| **TC-SFTP-07** | 一键下载至系统 Downloads | P0 | 选中远端文件，点击下载至本地「下载」目录 | 成功写入本地文件，校验文件大小与 MD5 完全一致 |
| **TC-SFTP-08** | 大文件传输稳定性 (1GB+ / 10GB+) | P0 | 上传/下载 5GB ISO 镜像文件 | 传输平稳，实时显示传输速度 (MB/s)、进度百分比与剩余时间，零 OOM 崩溃 |
| **TC-SFTP-09** | 传输中断与重连续传 | P1 | 在传输大文件到 50% 时模拟断网 3 秒后恢复 | 提示连接恢复，支持继续传输未完成数据，而非全部从零重传 |
| **TC-SFTP-10** | 同名文件覆盖冲突处理 | P1 | 上传同名文件到目标目录 | 弹出冲突处理弹窗（覆盖 / 跳过 / 重命名），行为符合选项 |
| **TC-SFTP-11** | 特殊字符与中文路径编码 | P0 | 上传名为 `测试 目录 #1 (v2.0) [正式].tar.gz` 的文件 | UTF-8 编码传输完整，无路径解析截断或乱码文件名 |
| **TC-SFTP-12** | 终端目录与 SFTP 联动 (OSC 7 / 633) | P1 | 在终端输入 `cd /tmp` 或 `cd /etc/nginx` | SFTP 面板自动跟随跳转至终端当前路径 |
| **TC-SFTP-13** | 轻量内置编辑器快速修改 | P1 | 双击远程文本文件 (如 `nginx.conf`)，编辑内容并 Cmd+S 保存 | 安全写入远端文件，自动备份，无需繁琐下载到本地再上传 |

---

### 模块 4：会话与资产管理系统 (Session Management & Sidebar)

| 用例 ID | 场景描述 | 优先级 | 测试步骤 / 验证方法 | 预期结果 |
| :--- | :--- | :---: | :--- | :--- |
| **TC-SESS-01** | **首次安装开箱洁净度 (Clean Slate)** | **P0** | 彻底清空数据目录全新启动应用 | **绝对不出现任何开发者的测试机、内网 IP (`100.64.x.x`) 或演示机器** |
| **TC-SESS-02** | 空白状态交互 (Empty State) | P1 | 在无任何会话时观察侧边栏 | 展示优雅的占位图标与文案，点击引导按钮顺畅唤起新建弹窗 |
| **TC-SESS-03** | 会话增删改查 (CRUD) 闭环 | P0 | 新建会话、修改主机/端口/账号、删除会话 | 数据准确落地 `sessions.json`，侧边栏即时响应更新 |
| **TC-SESS-04** | 会话快速克隆 (Duplicate) | P1 | 右键现有会话选择“复制会话” | 自动生成 `(副本)` 后缀会话，保留所有配置项供快速微调 |
| **TC-SESS-05** | 自定义分组与层级折叠 | P1 | 创建不同分组（生产环境/测试环境），折叠/展开分组 | 分组内机器准确收纳，折叠状态在切换标签后保持持久 |
| **TC-SESS-06** | 毫秒级多维度搜索过滤 | P0 | 在搜索框输入名称、IP 片段、标签或端口 | 列表实时毫秒级过滤呈现匹配项，清空搜索还原完整树 |
| **TC-SESS-07** | 会话配置安全导出与导入 | P1 | 将会话导出为 JSON 文件，清空后重新导入 | 资产配置无损还原，敏感密码安全脱敏或加密保存 |
| **TC-SESS-08** | 标签与机器状态高亮指示灯 | P2 | 给主机设置不同主题颜色（红/橙/绿/蓝） | 侧边栏及终端标签页准确显示对应机器颜色标识，便于环境识别 |
| **TC-SESS-09** | 防误删二次确认 | P1 | 点击删除正在使用或配置完整的会话 | 弹出模态二次确认弹窗，必须显式确认才执行物理删除 |

---

### 模块 5：进阶生产力工具 (Productivity & Advanced Tools)

| 用例 ID | 场景描述 | 优先级 | 测试步骤 / 验证方法 | 预期结果 |
| :--- | :--- | :---: | :--- | :--- |
| **TC-PROD-01** | 快捷命令片段 (Snippets) 一键执行 | P1 | 自建常用诊断命令（如 `df -h`、`docker ps`），点击运行 | 快速将命令无缝发送至当前焦点终端并执行输出 |
| **TC-PROD-02** | 模板变量动态插值替换 | P1 | 创建包含 `{{host}}`、`{{user}}` 的参数化快捷命令 | 点击运行时自动替换为当前活动会话的实际主机与用户名 |
| **TC-PROD-03** | 关键词高亮智能触发器 (Triggers) | P0 | 终端输出包含 `error`、`failed`、`exception` 或 IPv4 地址 | 关键词自动以醒目红色/蓝色背景高亮显示，正则匹配无遗漏 |
| **TC-PROD-04** | 多标签页高效管理 | P0 | Cmd+T 新建标签，按 Cmd+1~9 快速在多个活动会话间切换 | 标签切换毫秒级响应，各终端独立输入输出，无串流干扰 |
| **TC-PROD-05** | 活动会话防误关闭保护 | P0 | 在有正在前台运行命令（如 `ping`、`top`）的标签上按 Cmd+W | 弹出防误触弹窗提示，确认关闭后才断开连接释放资源 |
| **TC-PROD-06** | 键盘广播多路同屏执行 (Broadcast) | P0 | 打开 3 个同环境终端，开启广播输入栏输入 `uptime` | 回车后 3 个终端同时收到命令并同步输出执行结果 |
| **TC-PROD-07** | 无代理 (Agentless) 性能监控探针 | P0 | 连接标准 Linux/macOS 服务器，开启性能监控胶囊 | 无需在目标机安装任何 agent，自动采集 CPU%、内存使用、磁盘及上下行网速 |
| **TC-PROD-08** | 端口转发与 SSH 隧道 (-L / -R / -D) | P1 | 配置本地端口 8080 映射远端内网 80 服务 | 本地浏览器访问 `http://localhost:8080` 成功穿透访问远端内网服务 |
| **TC-PROD-09** | 终端会话日志落盘导出 | P2 | 点击导出当前会话完整交互历史 | 生成标准 UTF-8 文本或 HTML 格式日志文件 |

---

### 模块 6：UI / UX 与 macOS 原生系统适配 (UI, UX & macOS Integration)

| 用例 ID | 场景描述 | 优先级 | 测试步骤 / 验证方法 | 预期结果 |
| :--- | :--- | :---: | :--- | :--- |
| **TC-UI-01** | 深色 / 浅色模式原生无缝切换 | P0 | 在 macOS 系统设置中切换 Dark / Light 模式 | 界面文字、背景色、分割线、侧边栏即时自适应，无反白或文字不可读 |
| **TC-UI-02** | Retina 高分屏高刷 (120Hz ProMotion) | P0 | 在 MacBook Pro Liquid Retina XDR 屏幕上滚动大量文本 | 文本边缘极度锐利，无模糊晕色，滚动 120 FPS 丝般顺滑 |
| **TC-UI-03** | 外接 4K 显示器拖拽跨屏 | P1 | 将 App 窗口从笔记本屏幕拖拽到外接 4K 显示器并缩放 | 字体渲染 DPI 即时适配，无字体骤然变大或模糊现象 |
| **TC-UI-04** | macOS 原生标准快捷键 | P0 | 验证 Cmd+C、Cmd+V、Cmd+W、Cmd+Q、Cmd+F、Cmd+K 行为 | 快捷键严格遵循 macOS 人机交互准则 (HIG)，无按键冲突阻断 |
| **TC-UI-05** | 侧边栏与工作区分割条拖拽 | P1 | 鼠标拖拽调整侧边栏宽度，双击快速折叠 | 拖拽手势跟手顺畅，支持最小宽度与最大宽度限制 |
| **TC-UI-06** | 终端字体无级缩放 | P1 | 按 Cmd + '+' 放大，Cmd + '-' 缩小，Cmd + '0' 重置基准大小 | 终端行列尺寸根据字号变化即时触发重绘，字符不变形 |
| **TC-UI-07** | 异常错误友好中文提示 | P0 | 故意输入错误密码或无效 IP 测试报错弹窗 | 提示通俗易懂的中文诊断引导，严禁向普通用户抛出原始底层崩溃栈 |
| **TC-UI-08** | 焦点捕获与键盘导航 | P1 | 在弹出模态框中按 Tab 切换输入框，按 Esc 取消，按 Enter 提交 | 键盘焦点管理闭环，全键盘可操作 |

---

### 模块 7：性能压测、极限边界与稳定性场景 (Performance & Stress Testing)

| 用例 ID | 场景描述 | 压测手段与标准 | 预期达标基准 |
| :--- | :--- | :--- | :--- |
| **TC-PERF-01** | **海量字符洪峰冲刷测试 (Flood Test)** | 在终端执行 `yes "ApexTerm High Throughput Pipeline Stress Testing Payload"` 或 `find / -ls`，持续高频输出 60 秒 | 主线程绝对不可假死无响应，UI 帧率保持 ≥ 30 FPS，内存受限在 RingBuffer 范围内不无限制暴涨，随时按 `Ctrl+C` 立即停止 |
| **TC-PERF-02** | **高频刷新复杂控制流压测** | 执行 `cmatrix`、`htop -d 1` 或 `glances` 持续运行 30 分钟 | GPU 显存占用平稳，CPU 使用率低于 15%，无丢行或控制符解析错乱 |
| **TC-PERF-03** | **7x24 小时长时间保活无泄漏** | 保持 3 个活动会话挂机 24 小时，定时运行健康探针 | 驻留内存 (RSS) 增幅低于 20MB，0 内存泄漏 (Zero Leaks)，连接持续保活 |
| **TC-PERF-04** | **高并发多会话同时运行** | 同时打开 20 个活动 SSH 终端并发执行 `top` | App 总 CPU 占用低于 30%，内存低于 350MB，各标签响应延迟 < 20ms |
| **TC-PERF-05** | **极端弱网与丢包仿真** | 使用网络环境调节器模拟 300ms 延迟 + 25% 随机丢包 | 键盘敲击具有按键回显保护缓冲，SFTP 具备断线重连，不崩溃抛错 |
| **TC-PERF-06** | **冷启动与就绪响应时间** | 彻底杀掉进程后点击应用图标冷启动 | 到达可交互主界面时间 < 0.8s，启动瞬时无白屏闪烁 |
| **TC-PERF-07** | **后台待机功耗与能耗指标** | 将应用置于后台最小化待机 1 小时 | 待机 CPU 占用 < 0.2%，定时器唤醒频率 (Idle Wakeups) < 10 次/秒 |
| **TC-PERF-08** | **5 万行超大文本瞬时粘贴** | 剪贴板放入 50,000 行纯文本一次性粘贴到终端 | 启动缓冲分片写入管道，界面出现加载进度，绝对不抛 OOM 闪退 |

---

### 模块 8：版本发布安全与打包门禁 (Release Gate Checklist)

每次准备打包新版本时，必须逐条对照以下 Checklist：

- [ ] **RG-01 自动化测试门禁**：运行 `swift test`，所有单元测试与集成测试必须 100% Passed。
- [ ] **RG-02 干净数据源审计**：
  - 检查 `SessionStore.swift`，确保 **没有任何自动注入的测试服务器**（如 `macmini-vm`、`100.64.x.x`、`k8s-prod-*`）。
  - 确保默认 `folders` 为 `[]`，默认 `snippets` 为 `[]`。
- [ ] **RG-03 生产模式编译校验**：执行 `swift build -c release` 零错误、零破坏性警告。
- [ ] **RG-04 Apple 官方代码签名合规**：
  - 检查 bundle 内部所有二进制与可执行文件（含 `ApexTerm` 与内嵌 helper）。
  - 执行 `codesign -vvv --deep --strict .build/ApexTerm.app` 必须输出 `valid on disk` 与 `satisfies its Designated Requirement`。
- [ ] **RG-05 安装包完整性验证**：
  - 校验生成的 `.dmg` 磁盘映像能否在全新的 macOS 机器上挂载并直接拖拽至 `/Applications` 运行。
  - 校验解压 `.tar.gz` 独立包能否直接双击启动。
- [ ] **RG-06 发布前 10 分钟快速冒烟测试 (Smoke Test)**：
  1. 打开全新打包的 App，确认侧边栏为空白引导页（或仅有用户自己的会话），无乱七八糟的 mock 数据。
  2. 新建一台真实 SSH 服务器并连接，确认 1 秒内进入终端。
  3. 执行 `ls -la`、`top`、`clear`，验证终端字符与清屏正常。
  4. 切换到 SFTP 面板，浏览并下载一个文件，验证下载正常。
  5. 验证快捷键 Cmd+W 关闭标签，Cmd+Q 正常退出应用。

---

## 三、 历史高频 Bug 与排查指南 (electerm / FinalShell 经验总结)

1. **Bug 1: 终端持续输出海量日志导致 UI 完全死锁**
   - **根本原因**：直接在主线程向 Text 视图追加高频字符串，导致主事件循环卡死。
   - **防护手段**：必须使用后台 RingBuffer 环形缓冲区，限制最大行数，并按 16ms (60Hz) 或 8ms (120Hz) 批量合并冲刷到渲染层。
2. **Bug 2: 退出 `vim` 或 `htop` 后终端留下一屏乱码或光标丢失**
   - **根本原因**：未能正确响应终端退出备用缓冲区的 DEC 模式转义序列 (`\033[?1049l` / `\033[?25h`)。
   - **防护手段**：VTParser 必须拦截并处理备用屏幕恢复与显示光标控制码。
3. **Bug 3: 复制粘贴多行 Shell 命令意外被全部自动执行**
   - **根本原因**：未启用括号粘贴模式 (Bracketed Paste Mode)，换行符被当作即时回车。
   - **防护手段**：在检测到粘贴内容包含 `\n` 且远端开启保护时，包裹 `\033[200~` 和 `\033[201~` 序列。
4. **Bug 4: 中文字符和英文字符混排时，光标位置左右漂移**
   - **根本原因**：字体非等宽或东亚字符宽 (East Asian Width) 未计为双列宽 (2 columns)。
   - **防护手段**：严格使用 wcwidth 算法计算字符列宽，采用系统原生 SF Mono / Menlo 等宽字体渲染。
